Tenis AI v7.8A — Integrity Guard

ETAP A. Ta wersja NIE dodaje jeszcze Joint Builder Engine.

Naprawy:
- RET/W/O/DEF/ABD/ABN i częściowe wyniki są usuwane przed uczeniem Adaptive.
- Ten sam filtr czyści dane Serve Props.
- Jeśli historyczne dane mają best_of=5, wymagane są 3 wygrane sety do uznania meczu za ukończony.
- Player Tendencies nie używa już wyniku z tego samego dnia co analizowany mecz (źródło ma tylko dokładność dnia).
- Live Tennis API: best_of / best_of_sets / opis BO5 jest przechowywany, jeśli provider go poda.
- BO5 fail-safe: rynki pełnego meczu z silnika BO3 są N/D; 1. set zostaje.
- Serve Props BO5 = N/D do czasu dedykowanego modelu.
- Market Lab BO5 = tylko 1. set.
- Consensus / Serve / Form / Surface wyświetlają score jako XX/100 zamiast XX%.
- Adaptive pozostaje rozkładem modelowym w %, ale nadal nie jest gwarancją ani kursem buka.
- Nowe wpisy głównej historii dostają model_version v7.8A-adaptive-hygiene.
- GitHub Actions ma fail-closed Integrity Gate przed trackerami i deployem.

Integrity Gate sprawdza:
- sumy probability do 100%,
- komplementarność OVER + UNDER,
- monotoniczność OVER (wyższa linia nie może mieć większej szansy),
- rozkłady stanów po gemach,
- PBP clean_holds + paths_with_breaks,
- zakaz publikowania pełnych rynków BO3 przy BO5.

Najpierw:
  unzip -o tenis-ai-v7.8A-integrity-guard.zip
  python install_v78a.py
  python -m pytest -q

Dopiero jeśli 0 failed:
  rm tenis-ai-v7.8A-integrity-guard.zip
  git add .
  git commit -m "Tenis AI v7.8A Integrity Guard"
  git pull --rebase origin main
  git push origin main
